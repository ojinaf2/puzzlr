# Address of the room server that runs online games (see server/).
# Public, not a secret: rooms hold nothing sensitive and are only reachable
# with the six-character code. Committed so a Vercel build needs no manual
# environment setup. Update if the Worker is ever renamed or moved.
VITE_ROOM_SERVER=wss://puzzlr-rooms.playpuzzlr.workers.dev
